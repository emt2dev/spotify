<template>
    <RouterLink to="library">
        <div v-if="randomColor.color" :style="`background-color: ${randomColor.color};`"
            class="contrast-[2] brightness-[0.87] rounded-xl aspect-square relative overflow-hidden">
            <div class="text-white pt-3 pl-4 font-semibold text-[23px]">
                {{ category }}
                <img class="absolute contrast-[0.55] -right-3.5 bottom-0 max-w-[100px] rotate-[20deg]" :src="image || ''" alt="">
            </div>
        </div>
    </RouterLink>    
</template>

<script setup>
import uniqolor from 'uniqolor';
import { ref, toRefs } from 'vue';
import { RouterLink } from 'vue-router';

let randomColor = ref('');
randomColor.value = uniqolor.random();

const props = defineProps({
    image: String,
    category: String,
});

const { category } = toRefs(props);

</script>

<style scoped>

</style>